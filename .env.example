DATABASE_URL=
DIRECT_DATABASE_URL=
DB_SCHEMA=
# APP_ID is platform-owned: Preview/deployed runs get the agent-platform App
# UUID. local-app is a local-development placeholder only.
APP_ID=local-app
VITE_BASE_PATH=

# Tier0 SDK auth/connectivity env is injected automatically by the platform at
# deployment. Do not add TIER0_* values here or expose them in generated app UI.
